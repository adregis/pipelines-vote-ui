apiVersion: tekton.dev/v1
kind: PipelineRun
metadata:
  name: vote-app-dev-run
  annotations:
    pipelinesascode.tekton.dev/on-event: "[push]"
    pipelinesascode.tekton.dev/on-target-branch: "[master, main]"
    pipelinesascode.tekton.dev/max-keep-runs: "5"
    # Mapeia as Tasks locais para o PaC resolver automaticamente:
    pipelinesascode.tekton.dev/task: "[run-unit-tests, buildah-local, git-update-manifest]"
spec:
  taskRunTemplate:
    serviceAccountName: pipeline
  pipelineSpec:
    workspaces:
      - name: shared-workspace
    tasks:
      - name: fetch-repository
        taskRef:
          resolver: cluster
          params:
            - name: kind
              value: task
            - name: name
              value: git-clone
            - name: namespace
              value: openshift-pipelines
        workspaces:
          - name: output
            workspace: shared-workspace
        params:
          - name: url
            value: "{{ repo_url }}"
          - name: revision
            value: "{{ revision }}"

      - name: run-tests
        taskRef:
          name: run-unit-tests
        params:
          - name: CONTEXT_DIR
            value: "."
        workspaces:
          - name: source
            workspace: shared-workspace
        runAfter:
          - fetch-repository

      - name: build-image
        taskRef:
          name: buildah-local
        params:
          - name: IMAGE
            value: "image-registry.openshift-image-registry.svc:5000/pipelines-tutorial/{{ repo_name }}:{{ revision }}"
          - name: CONTEXT_DIR
            value: "."
          - name: DOCKERFILE
            value: "Dockerfile"
        workspaces:
          - name: source
            workspace: shared-workspace
        runAfter:
          - run-tests

      - name: update-gitops-manifest
        taskRef:
          name: git-update-manifest
        params:
          - name: git-config-repo
            value: "https://github.com/adregis/vote-app-config.git"
          - name: environment
            value: "dev"
          - name: app-name
            value: "{{ repo_name }}"
          - name: image-tag
            value: "image-registry.openshift-image-registry.svc:5000/pipelines-tutorial/{{ repo_name }}:{{ revision }}"
        workspaces:
          - name: source
            workspace: shared-workspace
        runAfter:
          - build-image

  workspaces:
    - name: shared-workspace
      volumeClaimTemplate:
        spec:
          accessModes:
            - ReadWriteOnce
          resources:
            requests:
              storage: 1Gi